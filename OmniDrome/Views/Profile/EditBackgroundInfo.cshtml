<div>
    <table>
        <tr>
            <td>
                <strong>Title:</strong>
            </td>
            <td>
                <input type="text" class="form-control" ng-model="BackgroundInfoModel.title" />
            </td>
        </tr>
        <tr>
            <td>
                <strong>Type:</strong>
            </td>
            <td>
                <select class="form-control" ng-model="BackgroundInfoModel.type">
                    <option value="">--Select --</option>
                    <option ng-repeat="type in Types" value="{{type.Id}}" ng-selected="type.Id == BackgroundInfoModel.type">{{type.Name}}</option>
                </select>
            </td>
        </tr>
        <tr>
            <td>
                <strong>Start Date:</strong>
            </td>
            <td>
                <input type="date" class="form-control" ng-model="BackgroundInfoModel.startDate" />
            </td>
        </tr>
        <tr>
            <td>
                <strong>End Date:</strong>
            </td>
            <td>
                <input type="date" class="form-control" ng-model="BackgroundInfoModel.endDate" />
            </td>
        </tr>
        <tr>
            <td>
                <strong>Description:</strong>
            </td>
            <td>
                <input type="text" class="form-control" ng-model="BackgroundInfoModel.description" />
            </td>
        </tr>
        <tr>
            <td>
                <button type="button" ng-click="UpdateBackgroundInfo();" class="btn btn-primary">Update</button>
            </td>
            <td>
                <button type="button" ng-click="Cancel();" class="btn btn-primary">Cancel</button>
            </td>
        </tr>

    </table>
</div>
